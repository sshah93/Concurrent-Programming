/* Project Description: The purpose of this assignment is to acquire some experience with UNIX mechanisms that are helpful in writing multi-process programs. Program called "primes" accepts a sequence of increasing positive numbers on the command line. The integers define range and a child process is created to search for primes in each range. */

#ifndef HEADER_H
#define HEADER_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <string.h>
#include <limits.h>

#endif